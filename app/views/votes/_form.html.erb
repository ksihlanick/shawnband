<div class="row center">
  
  <%= form_for(@vote) do |f| %>


  <%= f.hidden_field :song1, :value => @songs.first.key %>
  <%= f.hidden_field :song2, :value => @songs.last.key %>
  <%= f.hidden_field :ip, :value => @remoteip %>

  <div class="control-group">


    <div class="span4 left">
    <%= f.submit "Left!", class: "span3 btn btn-large btn-primary", :disable_with => "Please wait..." %>
    </div>


    <%= f.submit "Right!", class: "span3 btn btn-large btn-primary", :disable_with => "Please wait..." %>
    
	<!--
    <div class="span4 right">
      <%= f.submit "Skip!", class: "span3 btn btn-large btn-primary", :disable_with => "Please wait..." %>
    </div>
  -->


    <p></p>

    <div class="controls">
      <%= f.text_area :comment, :required => false, :placeholder => "Type a comment here!  Tell us anything!", :rows=>4 %>
    </div>
    
  </div>

  <% end %>
</div>