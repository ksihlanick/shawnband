<div class="row center">
  
  <%= form_for(@vote) do |f| %>


  <%= f.hidden_field :song1, :value => @songs.first.key %>
  <%= f.hidden_field :song2, :value => @songs.last.key %>
  <%= f.hidden_field :ip, :value => @remoteip %>

  <div class="control-group">


    <%= f.submit "Left!", class: "btn btn-large btn-primary", :disable_with => "Please wait..." %>

    <input class="btn btn-large btn-primary" onClick="history.go(0)" data-disable-with="Please wait..." name="commit" type="submit" value="Skip!" />
    
    <%= f.submit "Right!", class: "btn btn-large btn-primary", :disable_with => "Please wait..." %>



    <p></p>

    <div class="controls">
      <%= f.text_area :comment, :required => false, :placeholder => "Type a comment here!", :rows=>4 %>
    </div>
    
  </div>

  <% end %>
</div>